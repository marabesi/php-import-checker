<?php

/**
 * Should highlight the alias, as this is not being used in the code, this is
 * not likely to happen in real code, but the extension will handle that as if
 * it were..
 */

namespace Modules\Api\Models;

use Illuminate\Database\Eloquent\Model as Laravel;
use Illuminate\Database\Eloquent\Model;

class Step extends Model
{
}